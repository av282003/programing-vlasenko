int main()
{
        //Задание 20
        //a 1 аргумент, b второй аргумент
        int a = 50, b = 52, res;
        //вычисляем значение
        res = (a + b) / 2. * (b - a + 1);
        //выходим из программы
        return 0;
}
